const refs = {
  formEl: document.querySelector('.search-form'),
  searchBtn: document.querySelector('.search-btn'),
  galleryEl: document.querySelector('.gallery'),
};

export const { formEl, searchBtn, galleryEl } = refs;
